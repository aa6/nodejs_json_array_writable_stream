{
    "name": "test_suite",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "test": "node prestart.js && npx jasmine",
        "start": "node prestart.js && npm i . && npm run test"
    },
    "engines": {
        "node": ">=14.0.0"
    },
    "dependencies": {
        "main": "file:../..",
        "semver": "5.7.2",
        "jasmine": "^3.6.1",
        "jasmine-expect": "^5.0.0",
        "jasmine-spec-reporter": "^7.0.0"
    }
}
